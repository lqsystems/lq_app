import environmentControls from 'environmentControls.module';
// Modules

const modules = {
  entities: {
    modules: {},
    reactions: {},
  },
  sensors: {},
  ui: {
    environmentControls: {
      selectedControl: String,
    },
  },
};
