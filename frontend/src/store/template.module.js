/* eslint "no-shadow": "off" */
import Vue from 'vue';
import callApi from '@/utils/ApiUtils.js';

import {
} from './mutations.types';


const state = {
};

const mutations = {
};

const actions = {
};

const getters = {
};

export default {
  state,
  mutations,
  actions,
  getters,
};
